# ── Variáveis de ambiente do servidor (NÃO usam prefixo VITE_, então nunca vão
#    para o navegador). Defina-as na Vercel em Project Settings → Environment Variables.
#    Para rodar localmente com `vercel dev`, copie este arquivo para .env e preencha.

# Senha INICIAL do painel de administração (/admin). Depois de logar, você pode
# trocá-la pelo próprio painel (botão "Alterar senha") — a nova senha fica salva
# no Upstash Redis. Enquanto você não trocar, vale este valor.
ADMIN_PASSWORD=troque

# Segredo usado para assinar o cookie de sessão. Gere um valor longo e aleatório.
# Ex. (Node): node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=troque-por-um-valor-aleatorio-longo

# Armazenamento (Upstash Redis). Crie um banco grátis em https://upstash.com,
# abra o database → aba "REST API" → copie a URL e o token.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Cloudinary — onde ficam as fotos enviadas pelo admin. Crie uma conta grátis em
# https://cloudinary.com e copie os 3 valores do Dashboard (Account Details).
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
